Python中的List是用数组来实现的;
时间复杂度:   
	while: n
	  while: n
	    while: n
	3*n^2  n^2   -->  O(n^2)
	while n:     -->  O(n)

数组与链表的区别:
数组:可以使用下标索引,访问元素快O(1), 
      插入删除元素慢O(n);
链表:不使用下标索引,访问元素慢O(n), 
      插入删除元素快O(1);

正则表达式:
    最难的地方不是怎么匹配到数据,
而是怎么剔除不想要的数据;
    万能的正则:
	[\s\S]*?
万能的正则在匹配网页时,结合findall使用,不是真正的懒惰,
实际上是小步快跑的非贪婪,但是会匹配完所有的信息;
在使用时注意控制好开始点和结束点;
<div class="movie-item-info">[\s\S]*?title="([\s\S]*?)".*?class="star">(.*?)</p>.*?class="releasetime">(.*?)</p>.*?</div>

g-zhaojun@tedu.cn

Python --> driver --> 浏览器:
   有可能Python进程结束时,没有释放浏览器的进程;
有一个守护进程,定期去用kill -9等方式杀掉进程;


进程,线程,协程:
    线程是一个轻量级的进程,协程是轻量级的线程;
协程事实上会记录当前这个函数调用的位置和状态信息;
然后CPU的时间片转让给它的调用方;
斐波那契数列数列的非递归实现;

GIL: CPython
JPython, Pypy, 


作业:
    1)算法题:使用两个栈来构造一个队列;
	     使用两个队列来构造一个栈;
             

	
